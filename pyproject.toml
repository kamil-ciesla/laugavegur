[tool.poetry]
name = "laugavegur"
version = "0.1.0"
description = ""
authors = ["Kamil Cieśla"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
